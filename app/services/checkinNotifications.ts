// services/checkinNotifications.ts
// 🔒 Morning check-in feature is intentionally paused
// These are safe no-op stubs to satisfy imports during build

export async function scheduleDailyCheckIn() {
  // intentionally empty
  return;
}

export async function cancelDailyCheckIn() {
  // intentionally empty
  return;
}
